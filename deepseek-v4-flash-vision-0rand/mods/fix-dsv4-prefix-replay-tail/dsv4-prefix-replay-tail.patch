--- a/vllm/v1/core/single_type_kv_cache_manager.py
+++ b/vllm/v1/core/single_type_kv_cache_manager.py
@@ -472,6 +472,17 @@
         # retention: the replay boundary (``num_prompt - 1``, capped by
         # ``get_computed_blocks``) and any detected shared-prefix junction.
         reachable_boundaries = [request.num_prompt_tokens - 1]
+        # [fix-dsv4-prefix-replay-tail] With EAGLE-style drafting a sliding
+        # group's hit run needs a FULL peek block starting on the aligned
+        # boundary. A prompt ending 1..slack tokens past that boundary has no
+        # such block, so the tail kept above is unreachable and, under sparse
+        # retention, a replay or follow-up turn finds no hit at all. Also keep
+        # the tail at the last reachable boundary. This only adds retained
+        # blocks; the coordinator sets one slack on every manager because the
+        # hybrid hit is the minimum over groups.
+        slack = getattr(self, "replay_boundary_slack", 0)
+        if slack and request.num_prompt_tokens - 1 - slack > 0:
+            reachable_boundaries.append(request.num_prompt_tokens - 1 - slack)
         if request.shared_prefix_boundary:
             reachable_boundaries.append(request.shared_prefix_boundary)
 
--- a/vllm/v1/core/kv_cache_coordinator.py
+++ b/vllm/v1/core/kv_cache_coordinator.py
@@ -671,6 +671,22 @@
             manager.cache_hit_alignment_tokens = cache_hit_alignment_tokens
         self.verify_and_split_kv_cache_groups()
 
+        # [fix-dsv4-prefix-replay-tail] Replay-boundary slack: the largest block
+        # size of an EAGLE sliding-window group finer than the hit alignment
+        # (its peek block must be full). 0 when there is no such group.
+        replay_boundary_slack = max(
+            (
+                m.block_size
+                for m in self.single_type_managers
+                if m.use_eagle
+                and isinstance(m.kv_cache_spec, SlidingWindowSpec)
+                and m.block_size < cache_hit_alignment_tokens
+            ),
+            default=0,
+        )
+        for manager in self.single_type_managers:
+            manager.replay_boundary_slack = replay_boundary_slack
+
     @property
     def _cache_hit_alignment_tokens(self) -> int:
         # Fine-grained partial hits may return hash-block-aligned lengths;
